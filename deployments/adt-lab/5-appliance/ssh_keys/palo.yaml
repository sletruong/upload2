# Palo Alto VM-Series fleet SSH key — references the secret uploaded by keys_upload.tf.
# mode: reference = framework reads the existing secret; it does NOT create or
# own it. Secret format: {"private": "...", "public": "..."}.
# The `admin:` prefix is added automatically by the stack (vendor: palo_alto → admin).
- name:
    explicit: adt-lab-palo-ssh-key
  description: "ADT-Lab Palo Alto VM-Series day-0 SSH key (RSA-3072)"
  project_id: rteller-demo-svc-e265-aaac
  mode: reference
  storage: secret_manager
  path: adt-lab-palo-ssh-key
